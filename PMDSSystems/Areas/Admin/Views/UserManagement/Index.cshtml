@model List<Microsoft.AspNetCore.Identity.IdentityUser>

<h2>User Management (HR Panel)</h2>

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Email</th>
            <th>User Name</th>
            <th>Email Confirmed</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var user in Model)
        {
            <tr>
                <td>@user.Email</td>
                <td>@user.UserName</td>
                <td>@user.EmailConfirmed</td>
            </tr>
        }
    </tbody>
</table>